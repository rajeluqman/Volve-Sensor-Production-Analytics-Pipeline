# F01

See `simulation/faults/README.md` table for the one-line description and the real-risk
mirror. Repro steps: apply the described change to a `sim_`-prefixed copy of the relevant
script/table only (never the real `claudecatalog`/`VOLVE_DB` namespace — checked by
`simulation/check_isolation.py`), observe the downstream break, then run
`python simulation/faults/reset.py` to restore clean sim state.
