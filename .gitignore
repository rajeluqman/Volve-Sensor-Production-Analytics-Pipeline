# Credentials — JANGAN COMMIT
.env
*.env
credentials/
**/*credentials*
*.key
*.pem

# Data files
*.parquet
*.csv
*.xlsx
*.las
*.xml
data/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/
*.egg-info/
dist/
build/

# Databricks
.databricks/

# NiFi
NiFiMCP/config.yaml

# Logs
*.log
logs/
airflow/logs/

# Docker
docker-compose.override.yml

# Dev environment config
setup.sh
.mcp.json
docs/MCP_SETUP.md

# NOTE (2026-06-29 governance retrofit): CLAUDE.md, PROJECT_STATUS.md, .claude/, and .github/
# were previously listed above as ignored. They are now INTENTIONALLY COMMITTED — same pattern
# as the other 3 pipeline-retrofit repos (home-credit/olist/paysim) — this is a standalone,
# self-contained governance+learning lab by design, not project-config noise.

# AI dev kit (Codespaces tooling — not project code)
.ai-dev-kit/
.vscode/

# AWS CLI installer
aws/
awscliv2.zip

# IDE
.idea/

# OS
.DS_Store
Thumbs.db
